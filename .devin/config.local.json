{
  "permissions": {
    "allow": [
      "Exec(gh auth)",
      "Exec(git init)",
      "Exec(gh repo)",
      "Exec(gh api)",
      "Exec(sleep)",
      "Exec(node)",
      "Exec(git add)",
      "Exec(npx playwright)",
      "Exec(ls)",
      "Exec(mkdir)",
      "Exec(python3)",
      "Exec(find)",
      "Exec(sed)",
      "Exec(pkill)"
    ]
  }
}